<div class="listing">

<div id="title"><h1><strong>Category:</strong> <%= @category.name %></h1></div>
</br>
<ul>
<% @category.books.each do |book| %>
<li><%= link_to book.title, book %></li>
<% end %>
</ul>

  <% if user_signed_in? %> 
<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>

  <% end %>

</div>
